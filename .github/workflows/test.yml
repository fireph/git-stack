name: tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: syntax check
        run: python3 -c "import ast; ast.parse(open('git-stack').read())"
      - name: run tests
        run: bash test.sh
